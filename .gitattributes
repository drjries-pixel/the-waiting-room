# This project is edited from Windows and deployed from WSL (see README
# "Running it"). Normalize to LF in the repo so files don't arrive on the Linux
# side with CRLF, which breaks shell scripts and confuses diffs.
* text=auto eol=lf

# Keep the lockfile out of diffs and language stats.
package-lock.json linguist-generated=true -diff
